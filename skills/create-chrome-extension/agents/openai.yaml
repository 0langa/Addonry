interface:
  display_name: "Addonry: Create Browser Extension"
  short_description: "Build browser extensions through proof-driven repair loops"
  default_prompt: "Use $addonry:create-chrome-extension to build my Chrome and Firefox extension, repair gaps until every confirmed criterion has current proof, and package it when requested."

policy:
  allow_implicit_invocation: false
